<footer class="footer">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-6">
                <script>document.write(new Date().getFullYear())</script> © Hoist.
            </div>
            <div class="col-sm-6">
                <div class="text-sm-end d-none d-sm-block">
                    Raise the Flag with Hoist
                </div>
            </div>
        </div>
    </div>
</footer>
 @section scripts
 {

     <script>
        function updateLayoutMode() {
            const currentTime = new Date();
            const currentHour = currentTime.getHours();
            const htmlElement = document.querySelector('html');

            if (currentHour >= 6 && currentHour < 18) {
                htmlElement.setAttribute('data-layout-mode', 'light');
            } else {
                htmlElement.setAttribute('data-layout-mode', 'dark');
            }
        }

        document.addEventListener('DOMContentLoaded', updateLayoutMode);
     </script>
 }