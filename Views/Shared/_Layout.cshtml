<!doctype html>
<html lang="en" data-layout="horizontal" data-layout-style="default" data-sidebar="light" data-topbar="dark" data-layout-width="boxed">
<head>
    @Html.Partial("~/Views/Shared/_title_meta.cshtml")
    @RenderSection("styles", required: false)
    @Html.Partial("~/Views/Shared/_head_css.cshtml")
</head>
<body>
    <!-- Begin page -->
    <div id="layout-wrapper">
        @Html.Partial("~/Views/Shared/_menu.cshtml")

        <!-- ============================================================== -->
        <!-- Start right Content here -->
        <!-- ============================================================== -->
        <div class="main-content">

            <div class="page-content">
                <div class="container-fluid">
                    @Html.Partial("~/Views/Shared/_page_title.cshtml")

                    @RenderBody()
                </div>
            </div>
            @Html.Partial("~/Views/Shared/_footer.cshtml")
        </div>

    </div>
    <!-- END layout-wrapper -->
  @*  @Html.Partial("~/Views/Shared/_customizer.cshtml")*@

    @*@Html.Partial("~/Views/Shared/_vendor_scripts.cshtml")*@


    @*Function for js to automatically change to dark mode based on time of day *@
@*    <script>
        function updateLayoutMode() {
            const currentTime = new Date();
            const currentHour = currentTime.getHours();
            const htmlElement = document.querySelector('html');

            if (currentHour >= 6 && currentHour < 18) {
                htmlElement.setAttribute('data-layout-mode', 'light');
            } else {
                htmlElement.setAttribute('data-layout-mode', 'dark');
            }
        }

        document.addEventListener('DOMContentLoaded', updateLayoutMode);
</script>*@


    @RenderSection("scripts", required: false)


</body>


</html>

 @section scripts
 {

     
 }
